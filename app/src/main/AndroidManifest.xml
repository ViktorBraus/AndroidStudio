<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="eugene.example.helloapplication">

    <application
        android:name=".Timer.PusherApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Menu.main_Information_about_program"></activity>
        <activity android:name=".Menu.About_activity" />
        <activity
            android:name=".PlansCreating.PlansCreating"
            android:label="@string/title_activity_schedule"
            android:theme="@style/AppTheme" />
        <activity android:name=".Plans.Plans" />
        <activity
            android:name=".MainPage.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>